{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "admin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api-dev": "cp .env.local .env && nodemon api.js -e js",
    "api-dev-win": "copy .env.local .env && nodemon api.js -e js",
    "admin-dev": "cp .env.local .env && nodemon admin.js -e js"
  },
  "author": "Rahbar Khan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "joi": "^17.6.0",
    "mongoose": "^6.6.1",
    "socket.io": "^4.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
